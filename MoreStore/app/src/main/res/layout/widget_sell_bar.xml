<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    >

    <TextView android:id="@+id/startPrice"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginEnd="8dp"
        android:fontFamily="@font/manrope_bold"
        tools:text="2 000 ₽"
        />

    <TextView android:id="@+id/setDiscount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_rectangle_rouned4"
        android:paddingHorizontal="34dp"
        android:paddingVertical="13dp"
        android:text="@string/chat_button_discount"
        android:textColor="@color/green"
        android:fontFamily="@font/manrope_regular"
        android:textSize="16sp"
        android:drawablePadding="8.5dp"
        app:drawableStartCompat="@drawable/ic_coins" />

</LinearLayout>