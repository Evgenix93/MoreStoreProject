<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView android:id="@+id/text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/manrope_bold"
        android:textAlignment="center"
        android:textColor="@color/black"
        tools:text="Пока у вас нет избранных брендов, но вы можете выбрать их в каталоге"
        />

    <TextView android:id="@+id/action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="24dp"
        android:text="@string/favorite_listEmpty_actionText"
        android:textColor="@color/white"
        android:background="@drawable/bg_rectangle_rounded4_black"
        android:gravity="center"
        android:fontFamily="@font/manrope_bold"
        android:textSize="16sp"
        android:padding="12dp"
        />

</LinearLayout>