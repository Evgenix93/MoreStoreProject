<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingStart="14.5dp"
    android:paddingVertical="10.5dp"
    android:paddingEnd="12dp"
    android:background="@drawable/sel_group_bg"
    android:orientation="horizontal"
    android:checked="false"
    android:gravity="center_vertical"
    >

    <ImageView
        android:id="@+id/unreadIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:src="@drawable/ic_ellipse"/>

    <ImageView android:id="@id/icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="10.5dp"
        android:src="@drawable/ic_sticker_outline"
        />

    <TextView android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/manrope_regular"
        android:textColor="@color/sel_green_black"
        tools:text="Мои объявления"
        />

    <TextView android:id="@+id/count"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginStart="8dp"
        android:textSize="10sp"
        android:gravity="center"
        android:background="@drawable/sel_circle"
        android:textColor="@color/sel_green_gray"
        tools:text="12"
        tools:ignore="SmallSp" />

</LinearLayout>