<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    >

    <TextView android:id="@+id/myPrice"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        app:drawableStartCompat="@drawable/ic_coins"
        android:textSize="16sp"
        android:text="@string/chat_button_myPrice"
        android:paddingVertical="13dp"
        android:paddingHorizontal="26dp"
        android:background="@drawable/bg_rectangle_rouned4"
        android:textColor="@color/green"
        android:layout_marginEnd="8dp"
        android:fontFamily="@font/manrope_regular"
        android:gravity="center_horizontal"
        />

    <TextView android:id="@+id/buy"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_rectangle_rouned4"
        android:backgroundTint="@color/green"
        android:textColor="@color/white"
        android:paddingVertical="13dp"
        android:text="@string/chat_button_buy"
        app:drawableStartCompat="@drawable/ic_cart"
        android:drawablePadding="10dp"
        android:paddingHorizontal="39dp"
        app:drawableTint="@color/white"
        android:fontFamily="@font/manrope_bold"
        android:gravity="center_horizontal"
        />

</LinearLayout>